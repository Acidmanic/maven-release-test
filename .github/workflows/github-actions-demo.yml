
name: Release
on: [push]
jobs:
    Deploy_Project:
        runs-on: ubuntu-latest
        steps: 
            - run: mvn -version
            - run: ${{ github.workspace }}/deploy.sh
            - run: ls ${{ github.workspace }}/target